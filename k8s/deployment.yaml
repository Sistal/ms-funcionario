apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-funcionario
  namespace: sistal
  labels:
    app: ms-funcionario
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-funcionario
  template:
    metadata:
      labels:
        app: ms-funcionario
    spec:
      containers:
      - name: ms-funcionario
        # El pipeline de GitHub Actions (sed) actualizará este tag por el Commit SHA real
        image: us-central1-docker.pkg.dev/sistal-490016/sistal-docker-images/ms-funcionario:IMAGE_PLACEHOLDER
        imagePullPolicy: Always
        ports:
        - containerPort: 8080 # Puerto estándar de tu app Go
        
        # AQUÍ CONSUME EL CONFIGMAP MANUAL
        envFrom:
        - configMapRef:
            name: ms-funcionario-config
            # optional: true # Descomenta esto si quieres que el pod inicie aunque el configmap no exista
            
        # Buenas prácticas: Límites de recursos
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
